name: Manual CI/CD to Shared Server

# This is the standard release entry point: it publishes the ACR images built
# from main, then deploys their main tag.
on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: shared-weknora-ci-cd-main
  cancel-in-progress: false

jobs:
  validate-release-ref:
    name: Validate release ref
    runs-on: ubuntu-latest
    steps:
      - name: Require main
        env:
          REF: ${{ github.ref }}
        run: |
          set -euo pipefail
          [ "$REF" = "refs/heads/main" ] || {
            echo "Manual CI/CD deployments must run from the main branch; got $REF." >&2
            exit 1
          }

  build-images:
    name: Build and publish ACR images
    needs: validate-release-ref
    uses: ./.github/workflows/docker-image.yml
    with:
      registry: acr
    secrets: inherit

  deploy:
    name: Deploy main images to shared server
    needs: build-images
    uses: ./.github/workflows/manual-registry-deploy.yml
    with:
      version: main
    secrets: inherit
